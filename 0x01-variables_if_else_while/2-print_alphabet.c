#include <stdio.h>
/**
 * main - Printing alphabets
 *
 * Return: Always (0) Success
 */
int main(void)
{
	char alpbt;

	alpbt = 'a';
	while (aplbt = a, a <= z, a++)
	{
		putchar("%c\n", alpbt);
	}
	return (0);

}
